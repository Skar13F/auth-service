server:
  port: 8080

spring:
  datasource:
    driver-class-name: "org.postgresql.Driver"
    url: "jdbc:postgresql://localhost:5432/auth"
    username: "user name"
    password: "password"
  jpa:
    hibernate:
    ddl-auto: "create-drop"

jwt:
  secret: ""